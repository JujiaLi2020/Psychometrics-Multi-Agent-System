services:
  orchestrator:
    build: ../agents/orchestrator
    ports: ["8000:8000"]
    depends_on: [scoring, fairness]
  scoring:
    build: ../agents/scoring
    ports: ["8001:8001"]
  fairness:
    build: ../agents/fairness
    ports: ["8002:8002"]
  selection:
    build: ../agents/selection
    ports: ["8003:8003"]
